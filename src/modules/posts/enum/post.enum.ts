export enum PostStatus {
  DRAFT = 'draft',
  PUBLISHED = 'published',
  ARCHIVED = 'archived',
}

export enum PostCategory {
  NEWS = 'news',
  ANNOUNCEMENT = 'announcement',
  HEALTH_TIP = 'health_tip',
}
